# print a pair of null pairs

# Output:
# #addrs# = ((nil),(nil))
#

# Program:

begin
  pair(pair(int, int), pair(int, bool)) p = newpair(null, null) ;
  print p ;
  print " = (" ;
  pair(int, int) q = fst p ;
  print q ;
  print "," ;
  pair(int, bool) r = snd p ;
  print r ;
  println ")"
end
